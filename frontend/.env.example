# Copy to .env.local for local overrides, or set VITE_API_BASE in your
# hosting platform's environment variables UI (e.g. Vercel project settings).
# Falls back to http://localhost:8001 if unset.
VITE_API_BASE=https://your-backend-url.example.com
