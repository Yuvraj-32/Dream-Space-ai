"""CAD (.dwg / .dxf) input support for DreamSpace.

Kept in "cad update folder/" so the feature stays separate from the image
detection code in backend/. backend/main.py adds this folder to sys.path and
mounts `cad.routes.create_cad_router`.
"""
